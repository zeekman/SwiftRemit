# ============================================
# SwiftRemit Configuration
# ============================================
# Copy this file to .env and fill in your values
# Never commit .env files to version control!

# ============================================
# SMART CONTRACT CONFIGURATION
# ============================================

# Maximum fee in basis points (validation limit in contract)
# This is a constant in the Rust code (10000 = 100%)
# Documented here for reference - cannot be changed without recompiling contract
# Type: number (constant)
# MAX_FEE_BPS=10000

# Fee calculation divisor (basis points to percentage)
# This is a constant in the Rust code
# Documented here for reference - cannot be changed without recompiling contract
# Type: number (constant)
# FEE_DIVISOR=10000

# Event schema version
# This is a constant in the Rust code
# Increment when event structure changes to help indexers handle migrations
# Type: number (constant)
# SCHEMA_VERSION=1

# ============================================
# CLIENT CONFIGURATION
# ============================================

# Network: 'testnet' or 'mainnet'
# Determines which Stellar network to connect to
# Type: string (enum: 'testnet' | 'mainnet')
# Default: testnet
NETWORK=testnet

# RPC endpoint URL (must be HTTPS)
# The Soroban RPC server URL for submitting transactions and queries
# Type: string (URL)
# Default: https://soroban-testnet.stellar.org:443
RPC_URL=https://soroban-testnet.stellar.org:443

# Default fee in basis points (e.g., 250 = 2.5%)
# Used as the default platform fee for remittances
# Must be between 0 and 10000 (0% to 100%)
# Type: number (0-10000)
# Default: 250
DEFAULT_FEE_BPS=250

# Transaction fee in stroops
# The fee to pay for each transaction on the Stellar network
# Type: string (numeric)
# Default: 100000
TRANSACTION_FEE=100000

# Transaction timeout in seconds
# How long to wait before a transaction expires
# Type: number (positive integer)
# Default: 30
TRANSACTION_TIMEOUT=30

# Poll interval for transaction status in milliseconds
# How often to check if a pending transaction has been confirmed
# Type: number (positive integer)
# Default: 1000
POLL_INTERVAL_MS=1000

# USDC token decimal places
# Number of decimal places for USDC token amounts
# Type: number (positive integer)
# Default: 7
USDC_DECIMALS=7

# ============================================
# CONTRACT ADDRESSES
# ============================================

# Deployed SwiftRemit contract address
# The address of your deployed SwiftRemit smart contract
# Type: string (Stellar address)
# Required for client operations
SWIFTREMIT_CONTRACT_ID=

# USDC token contract address
# The address of the USDC token contract on Stellar
# Type: string (Stellar address)
# Required for client operations
USDC_TOKEN_ID=

# ============================================
# ACCOUNT SECRETS (TEST ACCOUNTS ONLY!)
# ============================================
# WARNING: Never commit real secrets to version control!
# These are for testing purposes only.
# Use test accounts with minimal funds.

# Admin account secret key
# Has permission to initialize, register agents, update fees, withdraw fees
# Type: string (Stellar secret key)
# Optional: only needed for admin operations
ADMIN_SECRET=

# Sender account secret key
# Used to create remittances
# Type: string (Stellar secret key)
# Optional: only needed for testing sender operations
SENDER_SECRET=

# Agent account secret key
# Used to confirm payouts
# Type: string (Stellar secret key)
# Optional: only needed for testing agent operations
AGENT_SECRET=

# ============================================
# DEPLOYMENT CONFIGURATION
# ============================================

# Deployer identity name
# The Stellar CLI identity to use for deployment
# Type: string
# Default: deployer
DEPLOYER_IDENTITY=deployer

# Initial fee for contract initialization (basis points)
# The platform fee to set when initializing the contract
# Must be between 0 and 10000 (0% to 100%)
# Type: number (0-10000)
# Default: 250
INITIAL_FEE_BPS=250

# ============================================
# FEATURE FLAGS
# ============================================

# Enable debug logging (true/false)
# Controls whether debug logs are enabled in the contract
# Type: boolean
# Default: true
ENABLE_DEBUG_LOG=true

# ============================================
# CONFIGURATION NOTES
# ============================================
#
# Environment Variable Precedence:
# 1. System environment variables (highest priority)
# 2. .env.local file (local overrides, gitignored)
# 3. .env file (project defaults, gitignored)
# 4. .env.example file (template only, not loaded)
#
# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Fill in required values (CONTRACT_ID, USDC_TOKEN_ID)
# 3. Optionally customize other values for your environment
# 4. Never commit .env files to version control
#
# For more information, see CONFIGURATION.md
